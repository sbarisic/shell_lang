# TODO

## Completed

- [x] Replace raw CLR `ToString()` value rendering with one shared descriptor-aware formatter for both REPL results and `print`. Format primitives, arrays, and Results recursively; expose only registered ShellLang members (including inherited members); never use CLR reflection; treat host types without registered members as opaque type names; exclude queries and commands; and handle cycles, depth limits, and member-getter failures safely.

## 0.1 hardening

- [ ] Harden nominal type hierarchy and inherited-symbol resolution. Make the mock-game catalog match the normative `EXAMPLE.md` hierarchy (`NavigationError` and `SpawnError` derive from `MapError`; `InventoryError` derives from `PlayerError`), and reject inherited member/query names from incomparable bases unless the derived type explicitly resolves the name. Collapse repeated declarations reached through diamonds, prefer genuinely more-derived declarations, and use one canonical resolved-symbol set for the binder, help, completion, and value formatting. Cover direct assignability, nearest-common-error inference, base-order independence, member/query collisions, diamonds, explicit derived resolution, and the complete bootstrap example.

- [ ] Enforce session-binding isolation across host operations. Reject public `ShellSession.SetBinding` and `RemoveBinding` calls while `IsExecuting` is true, while giving the evaluator internal commit/remove operations for ShellLang assignments. Apply the same guard when commands, queries, member getters, future constructor delegates, or type-value providers run; this does not prevent commands from changing their declared host state. Test same-type replacement, incompatible replacement, removal, and attempted binding mutation at every host boundary; rejected mutations must become contained host faults, and subsequent name reads must retain their compiled types.

- [ ] Make the hosting descriptor API accurately represent its type safety. Remove the unused generic parameters from `CommandBuilder.Input<T>`, `Argument<T>`, and `Output<T>` because only the supplied `ShellTypeId` defines those contracts; replace them with non-generic declarations and update all registrations and documentation. Keep generic APIs only where `T` is actually validated, such as typed value adapters, invocation extraction, and future `TypeDescriptorBuilder<T>` constructor factories. If typed port declarations are needed later, introduce catalog-validated `ShellType<T>` handles rather than accepting unrelated CLR types and type IDs.

- [ ] Establish one automated conformance and documentation gate. Move assertions into a standard test project discoverable by `dotnet test`, share the deterministic mock host with the executable bootstrap runner, preserve every existing intrinsic and integration case, and add minimal CI that builds, runs the discovered tests, and executes the full example. At the same time, repair the displayed grammar by defining `literal` and the contextual leading-dot form, replace invalid standalone `[] -> count` / `[] -> sum` examples with typed contexts, and compile executable documentation snippets where practical so the specifications cannot silently drift from the parser and binder.

## Maintainability

- [ ] Keep every hand-written C# source file below 900 physical lines, with approximately 700 lines as the normal target before splitting. Refactor by responsibility rather than arbitrary line ranges: separate bound nodes, binding, adaptation, and intrinsic binding from `Compiler.cs`; evaluator flow, adaptation, host invocation, and intrinsic execution from `Runtime.cs`; descriptor families and builders from `Descriptors.cs`; type-system, registration, and core/intrinsic catalog concerns from `Catalog.cs`; and conformance suites from `shell_lang_test/Program.cs`. Use cohesive internal types or partial classes where shared private state makes them appropriate, preserve namespaces and the public API, avoid duplicate helpers and circular ownership, and make no semantic changes as part of the split. Add a repository check that fails when a non-generated `.cs` file exceeds 900 lines, then run formatting, the build, all conformance suites, and the complete bootstrap example after the refactor.

## Next language capabilities

- [ ] Add one coherent type-expression model for host construction, explicit core conversion, and type-scoped values.

  - **Syntax and resolution:** Reuse the existing grammar for `Vector3(...)`, nested `Transform(...)`, `Float32(count)`, `String(player.health)`, `Vector3.zero`, `Quaternion.identity`, `Math.pi`, and `Weapon.values`. A type call resolves either the registered host type's one canonical constructor or an engine-owned core conversion; it never participates in command overload resolution. Reject invocable type names that collide with commands, command aliases, or intrinsics. Type-qualified value access follows the existing enum rule and resolves the type name before a same-spelled binding. Constructors and conversions are expressions, not pipeline stages, and reject `<-` entries.
  - **Descriptor model:** Give a nominal host `TypeDescriptor` at most one non-inherited `ConstructorDescriptor` with ordered arguments, an optional declared error, and a synchronous pure factory. Add genuinely typed `TypeDescriptorBuilder<T>.Constructor(...)` and `.FallibleConstructor(...)` helpers whose success value is a CLR `T` wrapped by the owning adapter. Add immutable `TypeValueDescriptor` entries for fixed or provider-backed read-only values. Do not discover CLR constructors, conversion operators, static fields, properties, setters, or object initializers through reflection; core types, enums, errors, output records, arrays, and Results do not receive host constructors.
  - **Conversions:** Define an explicit engine-owned matrix instead of CLR coercion. Support checked numeric conversions and invariant `String` conversion from booleans, numbers, and enums using ShellLang names rather than CLR `ToString()`. Statically guaranteed conversions return `T`; range- or representation-sensitive conversions return `Result<T,ConversionError>`, backed by a public core error containing source and target type information. Document precision and non-finite-float behavior. Do not add implicit coercion, string-to-number parsing, or overloaded host conversions; alternate host representations use named factory commands.
  - **Type values and enums:** Register values such as `Vector3.zero`, `Vector3.up`, `Color.white`, and `Physics.gravity` while keeping the qualified name read-only and non-invocable. The resulting value remains a normal expression and can be assigned or piped. Synthesize a reserved `values` entry for every enum so `Weapon.values` returns immutable `Array<Weapon>` in declared member order and can flow through a pipeline. This replaces the proposed `enum_values(Weapon)` special form because ShellLang types are compile-time names, not runtime values.
  - **Binding and runtime:** Reuse command-style positional/named argument validation and expected argument types, while rejecting unknown, duplicate, missing, positional-after-named, and explicit-input entries. Evaluate supplied arguments once in source order, propagate the first argument Result error without calling the factory, combine declared errors through nearest-common-error rules, preserve active array-index context, validate every success/error/provider result through its descriptor and adapter, and contain nulls, wrong types, undeclared errors, and delegate exceptions as host faults.
  - **Tooling and verification:** Extend type help and expression completion with constructor signatures, defaults, conversions, type values, enum values, return types, and declared errors, while keeping them out of pipeline command completion. Document purity, name resolution, defaults, nesting, failures, and the no-reflection boundary. Add `Vector3`, `Color`, `Quaternion`, and `Transform` fixtures and cover positional/named/defaulted/nested construction, conversions and boundaries, non-constructible types, collisions, evaluation order, fallible factories, Result propagation, provider failures, member access, formatting, arrays, command arguments, help, completion, and the full conformance runner.

- [ ] Make collection intrinsics descriptor-driven, then add `flatten` through that schema. Replace the scattered intrinsic switches with one immutable schema containing name, description, primary input shape, positional/named parameters, contextual-expression role, result construction, and numeric/equality/ordering constraints; use it for binding, diagnostics, applicability, help, completion, and a read-only public metadata projection. Migrate every existing intrinsic before adding one-level `flatten : Array<Array<T>> -> Array<T>`. `flatten` preserves outer and inner order, returns a new immutable array declared as `Array<T>`, propagates an outer Result normally, rejects non-nested arrays, and never recursively flattens deeper shapes. Verify exact help signatures, constraint-aware completion, empty and covariant nested arrays, order, Result propagation, formatting, and rejection outside the declared input shape.

## API discovery and scale

- [ ] Scale command discovery and naming in two stages. First, enrich `CommandDescriptor` with optional immutable category, source examples, aliases, introduction version, and structured deprecation metadata containing message, version, and replacement. Preserve compatibility through empty defaults; validate aliases against all callable names; bind aliases to canonical command identity; prefer canonical names in completion; expose all metadata through help; and add non-fatal compilation warnings so deprecated names do not invalidate a program. Only after real flat-name pressure appears, add semantic namespaces with an unambiguous qualifier such as `player::damage`, `world::find_entities`, or `audio::play` rather than `.`, which remains value-member access. Keep categories separate from namespaces, allow nested namespace segments, use aliases for flat-name migration, and define namespace descriptors and collision rules before changing the lexer, parser, binder, help, completion, diagnostics, and documentation. Test metadata immutability, alias invocation and collisions, warnings, completion annotations, catalog revisions, qualified direct and pipeline calls, and independence from same-spelled bindings or globals.

## Deferred event model

- [ ] Design host events separately before implementing `Stream<T>`. Prefer a hot, host-owned `Event<T>` for sources such as `player_joined`, with a new long-lived subscription API and disposable execution handle instead of changing synchronous one-shot `Execute`. Specify subscription lifetime, cancellation, serial ordering, scheduler/thread affinity, buffering/backpressure, session capture and mutation, Result/runtime/host-fault handling per event, array interaction, and permissions before accepting pipelines such as `player_joined -> give(item: StarterKit)`. Keep `Stream<T>` reserved for a possible broader cold/asynchronous sequence model, and require deterministic mock-event conformance tests before either type becomes public.

## Design guardrail

Do not add `for`, `while`, arbitrary user functions, general lambdas, or mutable collection operations merely to implement the roadmap. Prefer typed pipelines, registered construction and conversion, contextual collection expressions, and narrowly specified event operators. Any future general-control-flow proposal must demonstrate why these forms cannot express the use case and must be versioned separately from 0.1 hardening.
